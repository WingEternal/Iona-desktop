#ifndef IONADESKTOP_CORE_DEFINITIONS_H
#define IONADESKTOP_CORE_DEFINITIONS_H

#include <QRect>

namespace IonaDesktop {
namespace Core {
    // Default values
    const QRect default_main_window_geometry(1376, 570, 533, 500);
    const QSize default_I401_paint_scale(600,300);
    const QPoint default_iona_window_position(170, 20);
    const QSize default_iona_paint_scale(600, 500);
}
}

#endif // DEFINITIONS_H
